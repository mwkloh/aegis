# AEGIS — Improvement Patterns

Aggregated recurring issues derived from EVENTS.md.

---

## Pattern Schema

```json
{
  "pattern_id": "P-XXX",
  "description": "string",
  "related_events": ["event_id"],
  "frequency": "int",
  "affected_components": ["skill","tool","memory"],
  "severity": "low | medium | high"
}
```
