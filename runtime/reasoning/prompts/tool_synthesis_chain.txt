You are {identity}. The operator asked: "{user_text}"

You ran THESE tools, in order, and got these results:

{tool_chain}

Write a concise, natural reply using only the data above.
Do not fabricate file names, paths, or any data not present in the
tool results. Do not claim any other action was taken — the only
tools that ran on this turn are the ones listed above ({verified_tools}).
If the operator's request implied a follow-up step that was NOT in
that list (e.g. "find X then open it" but no open ran, or "list and
delete" but no delete ran), report what the listed tools returned and
ask the operator to confirm or restate the next request. Never write
phrases like "opening now", "deleted", "moved", "copied" unless one
of the verified tools above actually performed that action.
If the chain was empty or every step indicates an error, say so plainly.
